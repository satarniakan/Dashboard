# گزارش تست کامل صفحات — ۱۴۰۵/۰۷/۰۴

اجرای برنامه روی پورت ۵۲۹۴ با اکانت ادمین و به‌عنوان کاربر حرفه‌ای، تک‌تک صفحات رندر و توابع اصلی (فرم‌ها، اعتبارسنجی‌ها، جست‌وجوها، دکمه‌ها، دیالوگ‌ها) تست شد.

## خلاصه
- **۵۵ مسیر** بررسی شد؛ همه رندر سالم دارند (به‌جز مواردی که در همین تست پیدا و **همان‌جا رفع** شد).
- **۴ باگ واقعی** حین تست پیدا و رفع شد (بخش بعدی).
- چرخه‌ی کامل فروشگاه (انتشار محصول ← موجودی ← سبد ← تخفیف ← تسویه ← سفارش در ادمین) **پایان‌به‌پایان سبز** است.

## باگ‌هایی که حین تست پیدا و رفع شد

| # | صفحه | باگ | رفع |
|---|------|-----|-----|
| ۱ | `/profile` | کرش رندر: کامپوننت‌های مشترک `ValidationMessage` را بدون `EditContext` رندر می‌کردند و فیلدهای بدون `@bind-Value` هم `ValueExpression` نداشتند | گیت `EditContext` در هر ۶ کامپوننت مشترک + ورودی ساده‌ی جایگزین برای خارج از EditForm |
| ۲ | `/shop/orders` | کرش: `Order.UserId` از نوع `int` بود ولی شناسه‌ی کاربر Identity رشته‌ی GUID است | نوع به `string` تغییر کرد + مایگریشن `FixOrderUserIdToString` |
| ۳ | افزودن به سبد | ۵۰۰: `CartRepository.UpdateAsync` روی انتیتی تازه‌ساخته (کلید موقت) `Update()` می‌زد | گارد `Detached` — تغییرات با `CompleteAsync` ذخیره می‌شوند |
| ۴ | صفحات فروشگاه | خطای هم‌روندی DbContext بین کوئری شمارنده‌ی سبد در Layout و کوئری صفحه | شمارنده به جزیره‌ی تعاملی `CartBadge` منتقل شد (کوکی با JS خوانده می‌شود؛ کوکی سبد `HttpOnly=false` شد — فقط GUID ناشناس) |

## توضیح مختصر صفحات

### داشبورد و حساب کاربری
| مسیر | توضیح | نتیجه تست |
|------|-------|-----------|
| `/` | داشبورد مدیریتی: KPI فروش امروز/ماه، نمودار، پرفروش‌ها و فاکتورهای اخیر | رندر اوکی، اسکلت لودینگ دارد |
| `/profile` | ویرایش نام/ایمیل/آدرس (استان-شهر آبشاری) و تغییر رمز | ✅ پس از رفع باگ ۱ رندر و فرم کار می‌کند |
| `/audit-logs` | گزارش رویدادهای سیستم با جست‌وجو | رندر و جست‌وجو اوکی |

### محصولات و کاتالوگ
| مسیر | توضیح | نتیجه تست |
|------|-------|-----------|
| `/products` | فهرست صفحه‌بندی‌شده محصولات + جست‌وجوی زنده | ✅ جست‌وجوی «گل» پیدا کرد، عبارت ناموجود حالت خالی داد |
| `/products/add` | فرم افزودن محصول (بخش‌بندی‌شده، قیمت با حروف، عکس، بخش فروشگاه) | ✅ submit خالی هر ۳ خطا را زیر فیلد داد؛ ثبت واقعی موفق |
| `/products/edit/{id}` | همان فرم برای ویرایش + انتشار در فروشگاه و Slug | ✅ ذخیره سالم (بدون دوباره‌نویسی نام)؛ انتشار تست شد |
| `/catalog/categories` | مدیریت دسته‌ها (نام + Slug + والد) | ✅ دسته «گل‌های شاخه‌بریده» ثبت شد (Slug الزامی است) |
| `/catalog/attributes` | ویژگی‌ها با مقادیر چندخطی | ✅ ویژگی «رنگ» با ۳ مقدار ثبت شد |
| `/catalog/units` | واحدهای شمارش: افزودن/ویرایش درجای/حذف با دیالوگ تایید و گارد «در حال استفاده» | ✅ چرخه کامل اوکی؛ حذف واحدِ مصرف‌شده مسدود می‌شود |
| `/catalog/product-groups` و `/add` و `/{id}` | گروه محصول (Variant ها) با تصاویر | رندر اوکی |

### انبار (WMS)
| مسیر | توضیح | نتیجه تست |
|------|-------|-----------|
| `/warehouse/stock` | موجودی هر کالا در هر انبار + بَج کم‌موجودی | رندر اوکی |
| `/warehouse/history` | تاریخچه گردش کالاها | رندر اوکی |
| `/warehouse/warehouses` (+add) | مدیریت انبارها | ✅ «انبار فروش آنلاین» ثبت شد |
| `/warehouse/suppliers` (+add) | مدیریت تأمین‌کنندگان | ✅ تأمین‌کننده ثبت شد |
| `/warehouse/customers` (+add) | مدیریت مشتریان | ✅ اعتبارسنجی خالی درست و ثبت موفق |
| `/warehouse/purchase-receipts` (+add) | رسید خرید با اقلام چندردیفی | ✅ رسید ۲۰ عددی برای گل رز ثبت شد و موجودی رفت بالا |
| `/warehouse/sales` (+add و `/{id}`) | فاکتور فروش با اقلام و تخفیف | فرم و اعتبارسنجی اوکی (ثبت نهایی نیازمند موجودی) |
| `/warehouse/sales-returns`, `/internal-issues`, `/transfers`, `/scrap`, `/stock-counts` (+add ها) | گردش‌های منفی/انتقال/انبارگردانی | رندر و ساختار فرم‌ها اوکی |

### حسابداری
| مسیر | توضیح | نتیجه تست |
|------|-------|-----------|
| `/accounting/journal` (+`/{id}`) | دفتر روزنامه و جزئیات سند | رندر اوکی |
| `/accounting/trial-balance` | تراز آزمایشی | رندر اوکی |
| `/accounting/profit-and-loss` | سود و زیان با چاپ | رندر اوکی |
| `/accounting/customer-receipts` (+add) | دریافت از مشتری | رندر و فرم اوکی |
| `/accounting/supplier-payments` (+add) | پرداخت به تأمین‌کننده | رندر و فرم اوکی |
| `/accounting/financial-accounts` (+add) | صندوق/بانک | رندر و فرم اوکی |
| `/accounting/customer-statement`, `/supplier-statement`, `/overdue-installments` | گردش حساب‌ها و اقساط | رندر اوکی |

### مدیریت و فروشگاه
| مسیر | توضیح | نتیجه تست |
|------|-------|-----------|
| `/admin/users` (+add, `/{id}`) | کاربران و نقش‌ها | رندر اوکی |
| `/admin/roles` | ماتریس مجوزها | رندر اوکی |
| `/admin/seed-test-data` | تولید دیتای تستی (خطرناک؛ فقط نمایش) | رندر اوکی |
| `/admin/discounts` | کدهای تخفیف | ✅ کد WELCOME10 (۱۰٪) ساخته شد |
| `/admin/orders` (+`/{id}`) | سفارش‌های فروشگاه | ✅ سفارش تستی با وضعیت «در انتظار پرداخت» دیده شد |
| `/shop`, `/shop/products` | ویترین و فهرست با فیلتر GET | ✅ محصول منتشرشده نمایش داده شد |
| `/shop/p/{slug}` | صفحه محصول با گالری/قیمت/موجودی/JSON-LD | ✅ با slug کار می‌کند |
| `/shop/cart` | سبد خرید: تعداد/حذف/کد تخفیف/جمع | ✅ تخفیف ۵۰٬۰۰۰ از ۵۰۰٬۰۰۰ درست محاسبه شد |
| `/shop/checkout` | آدرس + ارسال + خلاصه + پرداخت | ✅ تا درگاه رفت و خطای پیکربندی روشن داد |
| `/shop/orders` (+`/{id}`) | سفارش‌های من | ✅ پس از رفع باگ ۲ |
| `/sitemap.xml`, `robots.txt` | SEO | ۲۰۰ اوکی |
| `/login`, `/verify-otp`, `/register`, `/login-password` | احراز هویت OTP-محور | ✅ لاگین واقعی چند بار انجام شد |
| `/counter`, `/weather`, `/not-found`, `/error` | صفحات قالبی نمونه | رندر اوکی |

## به‌روزرسانی ۱۴۰۵/۰۷/۰۵ — بخش دوم
- **دو تست شکست قدیمی ریشه‌ای حل شد** (۴۳/۴۳ سبز): هر دو تست قبل از معرفی `IStockValidator` و کسر اتمیک `DecreaseWithCheckAsync` نوشته شده بودند و وابستگی‌های جدید را mock نکرده بودند. حالا validator و `DecreaseWithCheckAsync` درست mock شده‌اند و تست فروش همچنین `RollbackTransactionAsync` را verify می‌کند.
- **داده‌های آزمایشی** (محصول، دسته، ویژگی، تأمین‌کننده، مشتری، انبار، رسید خرید، کد تخفیف و سفارش تستی) از دیتابیس پاک شدند — دیتابیس حالا تمیز است.

## مسائل منطقی/کدی — همه در ۱۴۰۵/۰۷/۰۵ اصلاح شدند

| # | مسئله | وضعیت |
|---|-------|-------|
| ۱ | هزینه‌ی ارسال هاردکد در OrderService | ✅ به `StoreOptions` (بخش `Store` در appsettings: PostShippingCost/CourierShippingCost/InPersonShippingCost) منتقل شد |
| ۲ | ستاره‌ی الزامی روی فیلدهای اختیاری nullable با Range | ✅ در `ValidationAttributeInspector`: فیلد nullable فقط با `[Required]` ستاره می‌گیرد |
| ۳ | لغو سفارش در هر خطای MarkPaidAsync | ✅ تفکیک شد: BusinessRuleException (کمبود موجودی) → لغو؛ خطای سیستمی → سفارش با یادداشت برای ادمین می‌ماند و انقضای خودکار شامل سفارش‌های دارای پرداخت موفق نمی‌شود |
| ۴ | شماره سفارش با تاریخ تقریبی | ✅ با `PersianCalendar` واقعی به وقت تهران |
| ۵ | پارامتر بلااستفاده customerEmail | ✅ حذف شد |
| ۶ | کارایی سبد (GetAllAsync) | ✅ `ProductRepository.GetByIdsAsync` اضافه و در CartService و OrderService استفاده شد |
| ۷ | شماره ادمین در سورس | ✅ از `Identity:FirstAdminPhoneNumber` (appsettings/User Secrets) خوانده می‌شود؛ ثابت قدیمی فقط fallback است |
| ۸ | OTP در لاگ | بدون تغییر کد — `FakeSmsSender` بدنه‌ی پیام را فقط در Development لاگ می‌کند و در Production خودکار بی‌صدا است؛ اتصال سرویس واقعی فقط یک پیاده‌سازی جدید برای `ISmsSender` می‌خواهد |
| ۹ | خلاصه‌ی Checkout با هزینه‌ی ارسال ثابت | ✅ صفحه‌ی تسویه تعاملی شد؛ با تغییر روش ارسال، هزینه و «قابل پرداخت» لحظه‌ای به‌روز می‌شود |
| ۱۰ | کوکی سبد HttpOnly=false | ✅ راه‌حل نهایی: بَج از endpoint سبک `GET /shop/cart/count` با fetch تغذیه می‌شود (خارج از مدار Blazor، بدون تداخل DbContext) و کوکی همیشه دوباره ست می‌شود تا کوکی‌های قدیمی هم ارتقا یابند |
| ۱۱ | OutputCache | ❌ عمداً اعمال نشد — فرم‌های فروشگاه توکن antiforgery در HTML دارند که با کش شدن با کوکی کاربر بعدی نمی‌خواند و فرم‌ها می‌شکنند؛ راه درست (انتقال فرم‌ها به جزیره) مستند شد در STORE-ROADMAP.md |
| ۱۲ | صفحات قالبی Counter/Weather | ✅ حذف شدند |

**نکته‌ی عملیاتی جدید:** فایل‌های استاتیک (theme.js و…) حالا با پارامتر نسخه (`?v=...`) لود می‌شوند؛ بعد از هر تغییر در این فایل‌ها، نسخه را در `App.razor` bump کنید.

## داده‌های آزمایشی که در دیتابیس مانده‌اند (در صورت تمایل پاک کنید)
محصول «گل رز هلندی» (منتشرشده، slug: `gol-roz-holandi`)، دسته «گل‌های شاخه‌بریده»، ویژگی «رنگ»، تأمین‌کننده «تأمین‌کننده گل تستی»، مشتری «مشتری تستی فروشگاه»، انبار «انبار فروش آنلاین»، یک رسید خرید، کد تخفیف «WELCOME10» و یک سفارش PendingPayment.
